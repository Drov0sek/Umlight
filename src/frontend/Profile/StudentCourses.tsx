type PropsType = {
    userId : number,
    role : string
}

const StudentCourses = ({userId, role} : PropsType) => {
    return (
        <section>
        </section>
    );
};

export default StudentCourses;